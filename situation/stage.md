# Stage

*The single most important file in the system. Every skill reads this first and changes what it
does based on it. `/onboard` sets it; re-run `/onboard` whenever it changes.*

**Current stage:** `[ not set — run /onboard ]`

One of:

- `pre-enlistment` — thinking about joining, or in the process (recruiter, MEPS, DEP).
- `in-service` — currently serving.
- `transitioning` — separation or retirement is in sight (you have a date, or a rough window).
- `veteran` — already separated or retired.

**Separation / target date (if known):** `[ ... ]`
*Drives the transition countdown in `/next-move`. Leave blank if it doesn't apply yet.*

**Last updated:** `[ ... ]`
